[project]
name = "vocaloid"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mlx-whisper>=0.4.3 ; platform_machine == 'arm64' and sys_platform == 'darwin'",
    "faster-whisper>=1.0.0 ; not (platform_machine == 'arm64' and sys_platform == 'darwin')",
    "qwen-tts>=0.1.1",
    "soundfile>=0.13.1",
]
